meta {
  name: Patch Claim
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/api/claims/{{claimId}}
  body: json
  headers: {
    Authorization: Bearer {{token}}
  }
  auth: inherit
}

body:json {
  {
    "title": "Updated Claim {{$randomInt}}",
    "description": "Updated"
  }
}

tests {
  test("should respond OK and be updated", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
