meta {
  name: Get Claim
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/claims/{{claimId}}
  body: none
  auth: none
}

tests {
  test("should respond OK", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
