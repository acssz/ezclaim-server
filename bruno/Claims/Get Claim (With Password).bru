meta {
  name: Get Claim (With Password)
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/claims/{{claimId}}?password={{claimPassword}}
  body: none
  auth: none
}

tests {
  test("should respond OK (anonymous with password)", function () {
    expect(res.getStatus()).to.equal(200);
  });
}

