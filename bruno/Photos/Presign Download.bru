meta {
  name: Presign Download (object may not exist)
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/photos/{{photoId}}/download-url
  body: none
  auth: inherit
}

tests {
  test("should respond 200 or 404", function () {
    expect([200, 404]).to.include(res.getStatus());
  });
}
