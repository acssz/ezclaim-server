meta {
  name: Create Photo Record
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/photos
  body: json
  auth: none
}

body:json {
  {
    "bucket": "{{photoBucket}}",
    "key": "{{photoKey}}"
  }
}

script:post-response {
  const p = res.getBody()
  bru.setVar("photoId", p.id)
}

tests {
  test("should respond OK", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
