meta {
  name: Get Claim (No Password)
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/claims/{{claimId}}
  body: none
  auth: none
}

tests {
  test("should respond unauthorized", function () {
    expect(res.getStatus()).to.equal(401);
  });
}
