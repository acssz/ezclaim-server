meta {
  name: Get Audit Event
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/audit-events/{{auditEventId}}
  body: none
  auth: inherit
}

tests {
  test("should respond OK", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
